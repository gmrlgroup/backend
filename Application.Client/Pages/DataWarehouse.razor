@page "/datawarehouse"
@rendermode @(new InteractiveWebAssemblyRenderMode(prerender: false))
@attribute [Authorize(Policy = PolicyNames.DataWarehouseRead)]
@using System.Net.Http.Json
@using Application.Client.Components
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Components.Authorization
@inject HttpClient Http
@inject NavigationManager Navigation
@inject AuthenticationStateProvider AuthStateProvider
@inject StateContainer StateContainer
@attribute [Authorize]

<PageTitle>Data Warehouse</PageTitle>

<style>
    .dw-root {
        display: flex;
        height: calc(100vh - 88px);
        background: #f9fafb;
        overflow: hidden;
        font-family: -apple-system, BlinkMacSystemFont, 'Inter', 'Segoe UI', sans-serif;
    }

    /* ── Sidebar ────────────────────────────────────── */
    .dw-sidebar {
        width: 240px;
        background: white;
        border-right: 1px solid #e5e7eb;
        display: flex;
        flex-direction: column;
        flex-shrink: 0;
        transition: width .2s ease;
        overflow: hidden;
    }
    .dw-sidebar-collapsed { width: 48px; }

    .dw-sidebar-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 12px 12px 12px 14px;
        border-bottom: 1px solid #e5e7eb;
        flex-shrink: 0;
        min-height: 48px;
    }
    .dw-sidebar-collapsed .dw-sidebar-header { justify-content: center; padding: 12px; }

    .dw-sidebar-title {
        display: flex; align-items: center; gap: 7px;
        font-size: 12px; font-weight: 600; color: #374151; letter-spacing: .01em;
    }
    .dw-sidebar-title svg { color: #6366f1; flex-shrink: 0; }

    .dw-toggle-btn {
        width: 28px; height: 28px;
        display: flex; align-items: center; justify-content: center;
        border: 1px solid #e5e7eb; border-radius: 6px;
        background: white; cursor: pointer; color: #6b7280;
        flex-shrink: 0;
    }
    .dw-toggle-btn:hover { background: #f3f4f6; color: #374151; }

    .dw-sidebar-body { flex: 1; overflow-y: auto; display: flex; flex-direction: column; }

    .dw-sidebar-search {
        position: relative;
        padding: 10px 12px;
        border-bottom: 1px solid #f3f4f6;
        flex-shrink: 0;
    }
    .dw-sidebar-search svg {
        position: absolute; left: 20px; top: 50%; transform: translateY(-50%);
        width: 13px; height: 13px; color: #9ca3af; pointer-events: none;
    }
    .dw-sidebar-search input {
        width: 100%; height: 32px; padding: 0 8px 0 28px;
        border: 1px solid #e5e7eb; border-radius: 6px;
        font-size: 13px; background: #f9fafb; color: #111827; outline: none;
        box-sizing: border-box;
    }
    .dw-sidebar-search input:focus { border-color: #6366f1; background: white; }

    .dw-sidebar-count {
        padding: 6px 14px 4px;
        font-size: 11px; font-weight: 600; text-transform: uppercase;
        letter-spacing: .07em; color: #9ca3af;
    }

    .dw-table-list { flex: 1; padding: 4px 8px 8px; }

    .dw-table-item {
        display: flex; align-items: center; gap: 8px;
        padding: 7px 8px; border-radius: 6px; cursor: pointer;
        transition: background .1s; user-select: none;
    }
    .dw-table-item:hover { background: #f3f4f6; }
    .dw-table-item-active { background: #eef2ff !important; }
    .dw-table-item svg { width: 14px; height: 14px; color: #9ca3af; flex-shrink: 0; }
    .dw-table-item-active svg { color: #6366f1; }

    .dw-table-item-text { display: flex; flex-direction: column; min-width: 0; }
    .dw-table-schema { font-size: 10px; color: #9ca3af; text-transform: uppercase; letter-spacing: .05em; line-height: 1.2; }
    .dw-table-name { font-size: 13px; color: #374151; font-weight: 500; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
    .dw-table-item-active .dw-table-name { color: #4f46e5; }

    .dw-sidebar-error {
        display: flex; align-items: flex-start; gap: 8px;
        margin: 12px; padding: 10px 12px;
        background: #fef2f2; border: 1px solid #fecaca; border-radius: 8px;
        font-size: 12.5px; color: #dc2626;
    }
    .dw-sidebar-error svg { flex-shrink: 0; margin-top: 1px; }

    .dw-sidebar-empty {
        padding: 24px 14px;
        font-size: 13px; color: #9ca3af; text-align: center;
    }

    .dw-sidebar-loading {
        display: flex; flex-direction: column; align-items: center; gap: 10px;
        padding: 32px 14px; color: #9ca3af; font-size: 13px;
    }
    .dw-spinner { width: 22px; height: 22px; border: 2px solid #e5e7eb; border-top-color: #6366f1;
        border-radius: 50%; animation: dw-spin .7s linear infinite; }
    @@keyframes dw-spin { to { transform: rotate(360deg); } }

    /* ── Main area ──────────────────────────────────── */
    .dw-main { flex: 1; overflow: hidden; display: flex; flex-direction: column; }

    .dw-welcome {
        flex: 1; display: flex; flex-direction: column;
        align-items: center; justify-content: center;
        gap: 12px; padding: 40px; text-align: center;
    }
    .dw-welcome-icon {
        width: 52px; height: 52px; border-radius: 12px;
        background: #eef2ff; display: flex; align-items: center; justify-content: center;
        color: #6366f1; margin-bottom: 4px;
    }
    .dw-welcome-title { margin: 0; font-size: 18px; font-weight: 700; color: #111827; letter-spacing: -.01em; }
    .dw-welcome-sub { margin: 0; font-size: 14px; color: #6b7280; max-width: 360px; line-height: 1.6; }

    /* ── Dark mode ─────────────────────────────────── */
    [data-theme="dark"] .dw-root { background: #0e1117; }
    [data-theme="dark"] .dw-sidebar { background: #181a20; border-right-color: #2b303b; }
    [data-theme="dark"] .dw-sidebar-header { border-bottom-color: #2b303b; }
    [data-theme="dark"] .dw-sidebar-title { color: #e6e9ef; }
    [data-theme="dark"] .dw-sidebar-title svg { color: #818cf8; }
    [data-theme="dark"] .dw-toggle-btn { border-color: #2b303b; background: #181a20; color: #9aa1ac; }
    [data-theme="dark"] .dw-toggle-btn:hover { background: #20242c; color: #e6e9ef; }
    [data-theme="dark"] .dw-sidebar-search { border-bottom-color: #23272f; }
    [data-theme="dark"] .dw-sidebar-search svg { color: #9aa1ac; }
    [data-theme="dark"] .dw-sidebar-search input { border-color: #2b303b; background: #12141a; color: #e6e9ef; }
    [data-theme="dark"] .dw-sidebar-search input:focus { border-color: #6366f1; background: #181a20; }
    [data-theme="dark"] .dw-sidebar-count { color: #9aa1ac; }
    [data-theme="dark"] .dw-table-item:hover { background: #20242c; }
    [data-theme="dark"] .dw-table-item-active { background: rgba(99,102,241,0.15) !important; }
    [data-theme="dark"] .dw-table-item svg { color: #9aa1ac; }
    [data-theme="dark"] .dw-table-item-active svg { color: #818cf8; }
    [data-theme="dark"] .dw-table-schema { color: #9aa1ac; }
    [data-theme="dark"] .dw-table-name { color: #e6e9ef; }
    [data-theme="dark"] .dw-table-item-active .dw-table-name { color: #a5b4fc; }
    [data-theme="dark"] .dw-sidebar-error { background: rgba(239,68,68,0.12); border-color: rgba(248,113,113,0.3); color: #f87171; }
    [data-theme="dark"] .dw-sidebar-empty { color: #9aa1ac; }
    [data-theme="dark"] .dw-sidebar-loading { color: #9aa1ac; }
    [data-theme="dark"] .dw-spinner { border-color: #2b303b; border-top-color: #6366f1; }
    [data-theme="dark"] .dw-welcome-icon { background: rgba(99,102,241,0.15); color: #818cf8; }
    [data-theme="dark"] .dw-welcome-title { color: #f3f4f6; }
    [data-theme="dark"] .dw-welcome-sub { color: #9aa1ac; }
</style>

@if (!canView)
{
    <div class="flex flex-col items-center justify-center h-64 text-gray-500 gap-2">
        <svg class="w-10 h-10 text-gray-300" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
        <p class="text-sm font-semibold text-gray-700">Access Denied</p>
        <p class="text-xs text-gray-400">You don't have permission to view the data warehouse.</p>
    </div>
}
else
{
<div class="dw-root">
    <!-- Sidebar -->
    <div class="dw-sidebar @(sidebarCollapsed ? "dw-sidebar-collapsed" : "")">
        <div class="dw-sidebar-header">
            @if (!sidebarCollapsed)
            {
                <div class="dw-sidebar-title">
                    <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><ellipse cx="12" cy="5" rx="9" ry="3"/><path d="M21 12c0 1.66-4 3-9 3s-9-1.34-9-3"/><path d="M3 5v14c0 1.66 4 3 9 3s9-1.34 9-3V5"/></svg>
                    Tables
                </div>
            }
            <button class="dw-toggle-btn" @onclick="ToggleSidebar">
                @if (sidebarCollapsed)
                {
                    <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><polyline points="9 18 15 12 9 6"/></svg>
                }
                else
                {
                    <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><polyline points="15 18 9 12 15 6"/></svg>
                }
            </button>
        </div>

        @if (!sidebarCollapsed)
        {
            <div class="dw-sidebar-body">
                @if (isLoadingTables)
                {
                    <div class="dw-sidebar-loading">
                        <div class="dw-spinner"></div>
                        <span>Loading tables...</span>
                    </div>
                }
                else if (tables.Any())
                {
                    <div class="dw-sidebar-search">
                        <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
                        <input @bind="tableSearchText" @bind:event="oninput" placeholder="Search tables..." />
                    </div>

                    <div class="dw-sidebar-count">@FilteredTables.Count tables</div>

                    <div class="dw-table-list">
                        @foreach (var table in FilteredTables)
                        {
                            <div class="dw-table-item @(selectedTable == table.FullName ? "dw-table-item-active" : "")"
                                 @onclick="@(() => SelectTable(table.FullName))">
                                <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M9 21V9"/></svg>
                                <div class="dw-table-item-text">
                                    <span class="dw-table-schema">@table.Schema</span>
                                    <span class="dw-table-name">@table.Name</span>
                                </div>
                            </div>
                        }
                    </div>
                }
                else if (!string.IsNullOrEmpty(errorMessage))
                {
                    <div class="dw-sidebar-error">
                        <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
                        <span>@errorMessage</span>
                    </div>
                }
                else
                {
                    <div class="dw-sidebar-empty">No tables found</div>
                }
            </div>
        }
    </div>

    <!-- Main area -->
    <div class="dw-main">
        @if (!string.IsNullOrEmpty(selectedTable))
        {
            <DataWarehouseTable
                TableName="@selectedTable"
                AllColumns="@columns"
                Data="@tableData"
                TotalRows="@totalRows"
                CurrentPage="@currentPage"
                TotalPages="@totalPages"
                PageSize="@pageSize"
                IsLoading="@isLoadingData"
                OnPageChanged="@OnPageChanged"
                OnPageSizeChanged="@OnPageSizeChanged" />
        }
        else
        {
            <div class="dw-welcome">
                <div class="dw-welcome-icon">
                    <svg width="24" height="24" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><ellipse cx="12" cy="5" rx="9" ry="3"/><path d="M21 12c0 1.66-4 3-9 3s-9-1.34-9-3"/><path d="M3 5v14c0 1.66 4 3 9 3s9-1.34 9-3V5"/></svg>
                </div>
                <h2 class="dw-welcome-title">Data Warehouse Explorer</h2>
                <p class="dw-welcome-sub">Select a table from the sidebar to browse its data.</p>
            </div>
        }
    </div>
</div>
}

@code {
    [SupplyParameterFromQuery(Name = "c")]
    private string? companyId { get; set; }

    private List<TableInfo> tables = new();
    private List<TableInfo> FilteredTables => string.IsNullOrWhiteSpace(tableSearchText)
        ? tables
        : tables.Where(t => t.FullName.Contains(tableSearchText, StringComparison.OrdinalIgnoreCase) ||
                           t.Name.Contains(tableSearchText, StringComparison.OrdinalIgnoreCase) ||
                           t.Schema.Contains(tableSearchText, StringComparison.OrdinalIgnoreCase)).ToList();

    private string selectedTable = string.Empty;
    private List<DataWarehouseTable.ColumnDefinition> columns = new();
    private List<Dictionary<string, object?>> tableData = new();
    private int totalRows = 0;
    private int currentPage = 1;
    private int totalPages = 0;
    private int pageSize = 100;

    private bool canView;
    private bool isLoadingTables = false;
    private bool isLoadingData = false;
    private bool sidebarCollapsed = false;
    private string tableSearchText = string.Empty;
    private string errorMessage = string.Empty;

    protected override async Task OnInitializedAsync()
    {
        var companyId = StateContainer.Company?.Id ?? "";
        var authState = await AuthStateProvider.GetAuthenticationStateAsync();
        // {companyId}_ADMIN is implicitly allowed by HasCompanyRole.
        canView = authState.User.HasCompanyRole(companyId, "VIEW_DATA");
        if (!canView) { isLoadingTables = false; return; }

        await LoadTablesAsync();
    }

    // The DataWarehouse endpoints are gated by the DataWarehouseRead policy, which resolves the
    // active company from the X-Company-Id header.
    private void EnsureCompanyHeader()
    {
        Http.DefaultRequestHeaders.Remove("X-Company-Id");
        if (!string.IsNullOrEmpty(companyId))
            Http.DefaultRequestHeaders.Add("X-Company-Id", companyId);
    }

    private async Task LoadTablesAsync()
    {
        EnsureCompanyHeader();
        isLoadingTables = true;
        errorMessage = string.Empty;

        try
        {
            var response = await Http.GetFromJsonAsync<List<TableInfo>>("/api/DataWarehouse/tables");
            if (response != null)
            {
                tables = response;
            }
        }
        catch (Exception ex)
        {
            errorMessage = $"Failed to load tables: {ex.Message}";
            Console.WriteLine($"Error loading tables: {ex}");
        }
        finally
        {
            isLoadingTables = false;
        }
    }

    private async Task SelectTable(string tableName)
    {
        if (selectedTable == tableName) return;

        selectedTable = tableName;
        currentPage = 1;
        await LoadTableDataAsync();
    }

    private async Task LoadTableDataAsync()
    {
        if (string.IsNullOrEmpty(selectedTable)) return;

        EnsureCompanyHeader();
        isLoadingData = true;
        errorMessage = string.Empty;

        try
        {
            var response = await Http.GetFromJsonAsync<DataTableResult>(
                $"/api/DataWarehouse/data?tableName={Uri.EscapeDataString(selectedTable)}&page={currentPage}&pageSize={pageSize}");

            if (response != null)
            {
                columns = response.Columns.Select(c => new DataWarehouseTable.ColumnDefinition
                {
                    Name = c.Name,
                    DataType = c.DataType,
                    IsVisible = true
                }).ToList();

                tableData = response.Rows;
                totalRows = response.TotalRows;
                totalPages = response.TotalPages;
                currentPage = response.CurrentPage;
            }
        }
        catch (Exception ex)
        {
            errorMessage = $"Failed to load table data: {ex.Message}";
            Console.WriteLine($"Error loading table data: {ex}");
        }
        finally
        {
            isLoadingData = false;
        }
    }

    private async Task OnPageChanged(int page)
    {
        currentPage = page;
        await LoadTableDataAsync();
    }

    private async Task OnPageSizeChanged(int newPageSize)
    {
        pageSize = newPageSize;
        currentPage = 1;
        await LoadTableDataAsync();
    }

    private void ToggleSidebar()
    {
        sidebarCollapsed = !sidebarCollapsed;
    }

    // DTOs matching the backend models
    public class TableInfo
    {
        public string Schema { get; set; } = string.Empty;
        public string Name { get; set; } = string.Empty;
        public string FullName { get; set; } = string.Empty;
    }

    public class DataTableResult
    {
        public List<ColumnInfo> Columns { get; set; } = new();
        public List<Dictionary<string, object?>> Rows { get; set; } = new();
        public int TotalRows { get; set; }
        public int CurrentPage { get; set; }
        public int PageSize { get; set; }
        public int TotalPages { get; set; }
    }

    public class ColumnInfo
    {
        public string Name { get; set; } = string.Empty;
        public string DataType { get; set; } = string.Empty;
    }
}
